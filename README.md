# 2048 для Yandex.Игры

Классическая игра 2048, адаптированная для платформы Yandex.Игры.

## 🎮 Что это такое?

Это полностью рабочий проект игры 2048 для Godot 4.4+, готовый к экспорту в Web и публикации на Yandex.Игры.

## 📁 Структура проекта

```
2048 Yandex/
├── .cursor/              # Настройки Cursor (правила и скиллы)
│   └── skills/          # Скиллы для разработки
├── scenes/              # Сцены Godot
│   ├── main/           # Главная сцена (entry point)
│   ├── game/           # Игровая сцена
│   └── tile/           # Сцена плитки
├── scripts/            # GDScript файлы
│   ├── game_manager.gd        # Главный контроллер игры
│   ├── yandex_sdk.gd          # Обёртка для Yandex SDK
│   ├── grid.gd                # Логика поля 4x4
│   ├── tile.gd                # Логика плитки
│   └── input_handler.gd       # Обработка ввода (клавиатура + свайпы)
├── project.godot       # Конфигурация Godot проекта
├── export_presets.cfg  # Настройки экспорта в Web
├── PRODUCT_SPEC.md     # Полное ТЗ игры
└── README.md           # Этот файл
```

## 🚀 Как начать?

### 1. Открыть проект в Godot

1. Скачайте и установите **Godot 4.4** или новее с [godotengine.org](https://godotengine.org/)
2. Запустите Godot
3. Нажмите **Import**
4. Выберите файл `project.godot` в этой папке
5. Нажмите **Import & Edit**

### 2. Запустить игру локально

1. Откройте проект в Godot
2. Нажмите **F5** или кнопку "Play" (▶️) в правом верхнем углу
3. Игра запустится в отдельном окне

### 3. Управление

- **На компьютере:** стрелки на клавиатуре (или WASD)
- **На телефоне:** свайпы в нужном направлении

## 📝 Как работает игра?

### Основные файлы

1. **game_manager.gd** — управляет общим состоянием игры:
   - Хранит текущий счёт
   - Сохраняет лучший счёт
   - Интегрируется с Yandex SDK

2. **yandex_sdk.gd** — работает с Yandex Games:
   - Вызывает `gameReady()` при запуске
   - Показывает рекламу после Game Over
   - Обрабатывает ошибки (игра не падает)

3. **grid.gd** — вся логика 2048:
   - Поле 4×4
   - Движение плиток
   - Объединение одинаковых значений
   - Проверка Game Over

4. **tile.gd** — отдельная плитка:
   - Хранит значение (2, 4, 8, ...)
   - Анимации (появление, движение, слияние)
   - Цвета в зависимости от значения

5. **input_handler.gd** — обработка ввода:
   - Клавиатура (стрелки)
   - Свайпы (мышь + touch)

## 🔧 Как редактировать?

### Изменить цвета плиток

Откройте `scripts/tile.gd`, найдите `TILE_COLORS`:

```gdscript
const TILE_COLORS: Dictionary = {
	2: Color("#eee4da"),    # Светло-бежевый
	4: Color("#ede0c8"),    # Бежевый
	8: Color("#f2b179"),    # Оранжевый
	# ... и так далее
}
```

Поменяйте цвета на свои (используйте hex-коды).

### Изменить размер поля

Откройте `scripts/grid.gd`, найдите:

```gdscript
const GRID_SIZE: int = 4  # Поменяйте на 5 для поля 5×5
```

⚠️ **Внимание:** размер поля влияет на сложность. 4×4 — стандарт.

### Изменить скорость анимаций

Откройте `scripts/tile.gd`, найдите функции анимаций:

```gdscript
# Появление плитки
tween.tween_property(self, "scale", Vector2.ONE, 0.1)  # 0.1 сек

# Движение плитки
tween.tween_property(self, "position", target_position, 0.12)  # 0.12 сек
```

Поменяйте числа (в секундах).

## 📦 Как экспортировать в Web?

### 1. Подготовка

1. Откройте проект в Godot
2. Меню: **Project → Export**
3. Если нет пресета "Web" — нажмите **Add** → **Web**
4. Выберите пресет "Web"

### 2. Настройки экспорта (КРИТИЧНО для Yandex)

В пресете "Web" проверьте:

- ✅ **Threads:** OFF (должно быть выключено!)
- ✅ **Export Type:** Regular
- ✅ **Initial Memory:** 256–384 MB
- ✅ **Canvas Resize Policy:** Adaptive

### 3. Экспорт

1. Выберите путь для экспорта (например, `build/index.html`)
2. Нажмите **Export Project**
3. Дождитесь окончания экспорта

### 4. Результат

В папке `build/` появятся файлы:
- `index.html` — основной файл
- `index.wasm` — игра (WebAssembly)
- `index.pck` — ресурсы
- `index.js` — загрузчик

## 🌐 Как загрузить на Yandex.Игры?

### 1. Подготовка архива

1. Откройте папку `build/`
2. Выберите ВСЕ файлы (index.html, index.wasm, index.pck, index.js)
3. Создайте **ZIP-архив** (не RAR!)
4. Размер архива должен быть < 50 MB

### 2. Загрузка на Yandex

1. Зайдите в [Кабинет разработчика Yandex.Игры](https://console.cloud.yandex.ru/folders)
2. Создайте новую игру или откройте существующую
3. Раздел **Загрузка билда**
4. Загрузите ZIP-архив
5. Дождитесь проверки модерации

### 3. Модерация

Yandex проверит игру на:
- ✅ Работоспособность
- ✅ Вызов `gameReady()`
- ✅ Отсутствие threads
- ✅ Корректный показ рекламы

Если всё правильно — игра будет опубликована.

## 🐛 Как тестировать локально?

### Вариант 1: Godot Editor

Просто нажмите **F5** в редакторе Godot.

⚠️ **Ограничение:** Yandex SDK не будет работать (это нормально для локальной разработки).

### Вариант 2: Локальный Web-сервер

1. Экспортируйте игру в `build/`
2. Запустите локальный сервер:

```bash
# Python 3
cd build
python -m http.server 8000

# Или используйте VS Code расширение "Live Server"
```

3. Откройте браузер: `http://localhost:8000`

⚠️ **Ограничение:** Yandex SDK не будет работать без заголовков HTTPS.

### Вариант 3: Chrome без CORS (для разработки)

```bash
chrome.exe --disable-web-security --user-data-dir="C:/temp/chrome_dev"
```

Откройте `http://localhost:8000` — SDK может работать.

## ❓ Частые вопросы

### Игра не запускается в Godot

- Убедитесь, что используете **Godot 4.4+**
- Проверьте консоль на ошибки (внизу редактора)

### Плитки не двигаются

- Проверьте, что сцена `tile.tscn` правильно настроена
- Убедитесь, что скрипт `tile.gd` прикреплён к сцене

### Реклама не показывается

- На локальной разработке — это нормально
- На Yandex.Играх — проверьте консоль браузера (F12)

### Game Over не срабатывает

- Проверьте логику в `grid.gd`, функция `_is_game_over()`
- Убедитесь, что сигнал `game_over` подключён

## 📚 Дополнительные материалы

- [Документация Godot 4](https://docs.godotengine.org/en/stable/)
- [Yandex.Игры — SDK](https://yandex.ru/dev/games/)
- [PRODUCT_SPEC.md](./PRODUCT_SPEC.md) — полное ТЗ игры
- [.cursorrules](./.cursorrules) — правила разработки

## 🆘 Помощь

Если что-то не работает:

1. Проверьте консоль Godot (внизу редактора)
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы на месте
4. Перезапустите Godot

## ✅ Чеклист перед публикацией

- [ ] Игра работает в Godot (F5)
- [ ] Экспорт в Web завершён без ошибок
- [ ] Threads выключены в настройках экспорта
- [ ] Размер билда < 50 MB
- [ ] Игра работает в браузере локально
- [ ] `gameReady()` вызывается (проверьте консоль)
- [ ] Реклама показывается после Game Over
- [ ] Управление работает (клавиатура + свайпы)
- [ ] Game Over определяется корректно
- [ ] Best Score сохраняется

---

**Удачи с первой игрой на Yandex.Игры! 🎮**
